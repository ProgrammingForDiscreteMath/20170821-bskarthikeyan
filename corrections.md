1. Please do not leave fragments lile ``L = [ for i in range(10)]`` in your code. They will produce errors. The file ``code.py`` should run without any errors when I type ``python code.py``.

2. Your solutions should simply modify the code given in the assignment. You have provided new lines of code without doscstrings.

3. In the function ``is_prime``, the function should return``True`` or ``False`` (a boolean value) **score 2/5**

4. Could not find solution to last problem.

**Score 7/15**